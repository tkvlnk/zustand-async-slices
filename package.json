{
  "name": "zustand-async-slices",
  "version": "1.2.1",
  "description": "",
  "exports": {
    "./*": {
      "types": "./dist/types/*.d.ts",
      "require": "./dist/cjs/*.js",
      "import": "./dist/esm/*.js",
      "default": "./dist/esm/*.js"
    }
  },
  "scripts": {
    "build": "pnpm run build:clean && pnpm run build:all",
    "build:all": "tsc -b tsconfig.cjs.json tsconfig.esm.json tsconfig.types.json",
    "build:clean": "rm -rf ./dist",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  },
  "peerDependencies": {
    "zustand": "^4.5.2"
  }
}
